# Секрет для подписи JWT. ОБЯЗАТЕЛЬНО замените на свой в .env (не коммитьте его).
JWT_SECRET=change_me_super_secret
# Необязательный ключ администратора для создания учеток с ролью teacher/admin при регистрации
ADMIN_KEY=optional_admin_key
# Порт backend (опционально)
# PORT=4000
